<div class="container">
    <div class="jumbotron"><h1>This is a jumbotron</h1>
    <form action="/scrape">
        <button type="submit" class="btn btn-primary">GET SOME NEWS</button>
    </form>
    </div>
    {{#each dbArticle}}
    <div class="card">
        <div class="card-header">{{title}}</div>
        <div class="card-body">{{teaser}}<br>{{link}}</div>
    </div>
    {{/each}}
</div>